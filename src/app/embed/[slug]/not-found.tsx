/**
 * Bare 404 for a missing or embedding-disabled page. The root not-found
 * ships marketing chrome, which must never appear inside a host site's iframe.
 */
export default function EmbedNotFound() {
  return null;
}
